interface:
  display_name: "Codex Project Pilot"
  short_description: "Coordinate portfolios, thread topology, and evidence"
  default_prompt: "Use $codex-project-pilot to audit my portfolio and thread topology, then route the safest next actions."
